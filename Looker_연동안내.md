# Looker Studio 연동 안내 (실시간 대시보드)

우리 구글시트를 Looker Studio에 연결해, 대형 모니터·현관용 실시간 대시보드를 만듭니다.
Looker Studio는 마우스로 차트를 만드는 도구라 코드가 필요 없습니다. 무료입니다.

준비물: 매화 RAS 구글시트, 학교 구글 계정. 약 20~30분.

---

## 왜 Looker Studio인가
- 우리 시트를 그대로 데이터 소스로 읽어, 참여율·폰OFF율을 자동 차트로.
- 대형 화면에 실시간 표시(자동 새로고침). 발표에서 전문적으로 보임.
- dashboard.html(운영·의사결정)과 병행 — Looker는 '보여주기', dashboard는 '결정하기'.

## 1. Looker Studio 열기
1. lookerstudio.google.com 접속 → 학교 구글 계정 로그인.
2. "빈 보고서" 만들기.
3. 데이터 소스 연결 창이 뜨면 **Google Sheets** 선택.
4. 우리 "매화 RAS 데이터" 스프레드시트 → **daily** 시트 선택 → 추가.

## 2. 계산 필드 만들기 (참여율·폰OFF율)
daily에는 인원(R,A,S,폰프리)과 정원이 있습니다. 비율은 계산 필드로 만듭니다.
데이터 소스 편집 → 필드 추가:

| 필드 이름 | 수식 |
|---|---|
| R참여율 | R / 정원 |
| A참여율 | A / 정원 |
| S참여율 | S / 정원 |
| 폰OFF율 | 폰프리 / 정원 |

형식을 백분율(%)로 지정.

## 3. 차트 배치 (추천 구성)
보고서에 아래를 끌어다 놓습니다.

1. **스코어카드 4개** (큰 숫자)
   - R참여율 평균 · A참여율 평균 · S참여율 평균 · **폰OFF율 평균**
2. **시계열 차트** — 날짜별 R·A·S·폰OFF율 추세
3. **막대 차트** — 학급별 참여율 (기간 필터: 이번 주)
4. **기간 컨트롤** — 이번 주/이번 달 전환

폰OFF율은 색을 진하게(강조) 하면 슬로건이 눈에 띕니다.

## 4. 실시간·현관 표시
- 보고서 우상단 **데이터 새로고침** 주기를 15분/1시간으로 설정(파일→보고서 설정).
- **보기 모드**로 전환해 전체화면(F11)으로 현관·교무실 모니터에 띄우기.
- 공유 → "링크가 있는 모든 사용자 보기"로 하면 어느 기기에서나 열림(단, 학생 개인정보 없는 집계라 안전).

## 5. tree.html과 병행
- 현관 대형 화면: 왼쪽 tree.html(감성적 상징) + 오른쪽 Looker(수치·추세) 나란히.
- 또는 화면 전환(슬라이드쇼)으로 번갈아 표시.

## 6. 주의
- Looker는 시트를 '읽기'만 합니다. 데이터 입력·수정은 우리 시스템(check·dashboard)에서.
- 학급별 낮은 수치를 외부 공개 화면에 띄울 땐, 학부모·외부용은 '전교 평균'만 보이게 필터.
- 시트 열 이름(R, A, S, 폰프리, 정원, 날짜, 학급)을 바꾸면 Looker 필드도 다시 맞춰야 함.

## 발표 팁
"구글시트에 쌓인 학생 자가체크 → Looker Studio 실시간 시각화 → 현관 대형 화면"의 흐름을 보여주면,
'폰 OFF, RAS ON'이 데이터로 증명되는 장면이 됩니다. 특히 **폰OFF율 추세**가 올라가는 그래프가 핵심입니다.
