# AI 중계 서버 설치 안내 (Cloudflare Workers)

화면에 API 키를 넣지 않고, 키를 숨긴 서버가 대신 Upstage에 요청하게 만듭니다.
잠금 2가지(우리 사이트만 허용 + 암호)가 들어 있어, 주소가 공개돼도 남이 함부로 못 씁니다.

준비물: Upstage API 키, 이메일. 설치 프로그램 없음(웹에서 다 함). 약 15분.

---

## 1. Cloudflare 가입
1. dash.cloudflare.com 접속 → 무료 가입(이메일 인증).

## 2. Worker 만들기
1. 왼쪽 메뉴 **Workers & Pages** → **Create application** → **Create Worker**.
2. 이름을 정합니다(예: maehwa-ai). **Deploy**를 누릅니다(일단 기본 코드로).
3. 배포되면 **Edit code**를 누릅니다.
4. 편집기의 기존 코드를 모두 지우고, `cloudflare-worker.js` 내용을 전부 붙여넣습니다.
5. 오른쪽 위 **Deploy**.

## 3. 비밀값(키·암호) 등록
1. 그 Worker 화면에서 **Settings → Variables and Secrets**(또는 Variables)로 갑니다.
2. 아래 3개를 추가합니다. 키·암호는 **Encrypt(비밀)**로 저장하세요.

| 이름 | 값 | 설명 |
|---|---|---|
| UPSTAGE_KEY | up_... | Upstage 콘솔의 API 키 |
| GATE | maehwa-gate-8821 | 학교만 아는 암호(아무거나 정하세요) |
| ALLOWED_ORIGIN | https://아이디.github.io | 우리 사이트 주소(github.io 도메인) |

3. **Save / Deploy**.

> ALLOWED_ORIGIN은 사이트의 **도메인까지만**입니다. 저장소 경로(/ras2 등)는 빼세요.
> 예: 사이트가 https://flyingedukang-dev.github.io/ras2/ 면 → `https://flyingedukang-dev.github.io`

## 4. 서버 주소 확인
Worker 화면 위쪽에 주소가 있습니다: `https://maehwa-ai.OOO.workers.dev`
이게 화면에 넣을 **중계 서버 주소**입니다.

## 5. 화면에 연결
1. AI 화면(별이 사서, 대시보드 AI 보좌관 등)을 엽니다.
2. 상단 배너 **설정**을 누릅니다.
3. **중계 서버 주소**에 4번 주소를, **암호**에 GATE에 넣은 값을 입력 → 저장.
4. "✓ AI 연결됨 (중계 서버)"이 뜨면 됩니다.

이제 화면에는 API 키가 없습니다. 공개돼도 안전하고, 암호와 우리 사이트가 아니면 서버가 거부합니다.

---

## 확인·문제 해결
- **"암호가 맞지 않습니다"**: 화면에 넣은 암호와 GATE 값이 다릅니다.
- **"허용되지 않은 출처입니다"**: ALLOWED_ORIGIN이 실제 사이트 도메인과 다릅니다. 경로 없이 도메인만인지 확인.
- **"중계 실패"**: UPSTAGE_KEY가 틀렸거나 결제/한도 문제. Upstage 콘솔 확인.
- 로컬(file://)에서 열면 출처가 없어 막힐 수 있습니다. 깃허브 사이트 주소로 여세요.

## 비용
- Cloudflare Workers 무료 플랜: 하루 10만 요청까지 무료(학교 규모엔 충분).
- Upstage: 실제 요청량만큼 과금(Solar mini/pro2 기준 요청당 소액).

## 운영 팁
- 암호(GATE)는 가끔 바꾸고, 바뀌면 각 기기 화면 설정에서 다시 넣으면 됩니다.
- 학생 대면 AI(별이 사서 등)는 미성년자 약관 문제로, 실제 운영 시 교사 매개(교사 기기에서만) 방식을 권합니다.
