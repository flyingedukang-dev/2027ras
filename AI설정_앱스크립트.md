# AI 설정 (앱스 스크립트 통일 버전)

이제 별도 서버(Cloudflare) 없이, **구글시트에 이미 배포한 앱스 스크립트 하나**로 AI까지 처리합니다.
Upstage 키는 스크립트 속성에 숨겨져 화면에 노출되지 않습니다.

## 설정 (한 번만)

1. 구글시트 → 확장 프로그램 → Apps Script
2. 최신 `AppsScript_code.gs` 내용이 붙여져 있는지 확인 (AI 중계 기능 포함본)
3. 왼쪽 톱니바퀴 **프로젝트 설정** → 아래 **스크립트 속성** → **속성 추가**
   - 속성 이름: `UPSTAGE_KEY`
   - 값: Upstage API 키 (up_... 또는 콘솔에서 발급한 키)
   - 저장
4. **배포 → 배포 관리 → 연필 → 버전 "새 버전" → 배포** (기능 추가 후 꼭 재배포)

이게 끝입니다. 화면에는 AI 키를 넣지 않습니다.

## 화면에서

AI 화면(별이 사서·주간 편지·대시보드 보좌관 등)은 **구글시트에 연결돼 있으면 AI도 자동으로 됩니다.**
따로 AI 설정을 안 해도 되고, 시트 URL 하나로 통일됩니다.

- 시트에 연결됨(✓) → AI 버튼 누르면 Solar가 답합니다.
- 시트 미연결 또는 키 미설정 → 각 화면은 샘플 응답으로 시연됩니다.

## 장단점 (왜 이 방식인가)

- 장점: 서버를 따로 안 만듦, URL 하나로 통일, 키는 서버에만 있어 안전.
- 단점: 앱스 스크립트 특성상 AI 응답이 몇 초 더 걸리고, 하루 실행 한도가 있습니다.
  교사 위주의 사용 규모에는 충분합니다. 학생 수백 명이 동시에 AI를 부르는 용도로는 부적합합니다.

## 주의

- 학생 대면 AI(별이 사서 등)는 사용하지 않기로 했습니다. AI는 교사·관리자 도구로만 씁니다.
- 생성형 AI 활용은 연구보고서에 그 사실과 약관 준수(교사 계정·서버 경유)를 밝히세요.
- Cloudflare 방식(cloudflare-worker.js)은 이제 필요 없습니다. 빠른 응답·대량 사용이 필요할 때만 대안으로 참고하세요.
